<!-- Static-map navigation: compute once per attempt. Every retry necessarily
     returns through ComputePathToPose before FollowPath, so a cleared/empty
     path can never be sent to the controller. -->
<root BTCPP_format="4" main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <RecoveryNode number_of_retries="3" name="NavigateRecovery">
      <SequenceWithMemory name="PlanThenFollow">
        <ControllerSelector selected_controller="{selected_controller}" default_controller="FollowPath" topic_name="controller_selector"/>
        <PlannerSelector selected_planner="{selected_planner}" default_planner="GridBased" topic_name="planner_selector"/>
        <RecoveryNode number_of_retries="1" name="PlanRecovery">
          <ComputePathToPose goal="{goal}" path="{path}" planner_id="{selected_planner}" error_code_id="{compute_path_error_code}"/>
          <ClearEntireCostmap service_name="global_costmap/clear_entirely_global_costmap"/>
        </RecoveryNode>
        <FollowPath path="{path}" controller_id="{selected_controller}" error_code_id="{follow_path_error_code}"/>
      </SequenceWithMemory>
      <ReactiveFallback>
        <GoalUpdated/>
        <Sequence>
          <RoundRobin>
            <Sequence>
              <ClearEntireCostmap service_name="local_costmap/clear_entirely_local_costmap"/>
              <ClearEntireCostmap service_name="global_costmap/clear_entirely_global_costmap"/>
            </Sequence>
            <Wait wait_duration="2.0"/>
            <BackUp backup_dist="0.20" backup_speed="0.10"/>
          </RoundRobin>
          <!-- SequenceWithMemory retries FollowPath directly. Always refill
               {path} after a recovery action so an empty path is impossible. -->
          <ComputePathToPose goal="{goal}" path="{path}" planner_id="{selected_planner}" error_code_id="{compute_path_error_code}"/>
        </Sequence>
      </ReactiveFallback>
    </RecoveryNode>
  </BehaviorTree>
</root>
